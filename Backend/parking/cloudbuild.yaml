# ============================================================
# Cloud Build — Pipeline CI/CD
# ============================================================

steps:

  # ── Paso 1: Tests unitarios ───────────────────────────────
  - name: 'maven:3.9-eclipse-temurin-24'
    id: 'unit-tests'
    entrypoint: 'mvn'
    args:
      - 'test'
      - '-q'

  # ── Paso 2: SonarCloud ────────────────────────────────────
  - name: 'maven:3.9-eclipse-temurin-24'
    id: 'sonar-analysis'
    entrypoint: 'mvn'
    args:
      - 'sonar:sonar'
      - '-Dsonar.projectKey=jorgeg1824_parking-system'
      - '-Dsonar.organization=jorgeg1824'
      - '-Dsonar.host.url=https://sonarcloud.io'
      - '-Dsonar.token=$$SONAR_TOKEN'
    secretEnv:
      - 'SONAR_TOKEN'
    waitFor:
      - 'unit-tests'

  # ── Paso 3: Build Docker ──────────────────────────────────
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-image'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/parking-repo/parking-backend:$COMMIT_SHA'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/parking-repo/parking-backend:latest'
      - '.'
    waitFor:
      - 'sonar-analysis'

  # ── Paso 4: Push Artifact Registry ────────────────────────
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-image'
    args:
      - 'push'
      - '--all-tags'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/parking-repo/parking-backend'
    waitFor:
      - 'build-image'

# ── Paso 5: Deploy en Cloud Run con secretos ─────────────────────────────
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-cloud-run'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'parking-backend'
      - '--image=us-central1-docker.pkg.dev/$PROJECT_ID/parking-repo/parking-backend:$COMMIT_SHA'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--add-cloudsql-instances=$PROJECT_ID:us-central1:parking-db'
      - '--set-secrets=DB_PASSWORD=DB_PASSWORD:latest'
      - '--set-secrets=DB_USER=DB_USER:latest'
      - '--set-secrets=DB_NAME=DB_NAME:latest'
      - '--set-secrets=CLOUD_SQL_CONNECTION=CLOUD_SQL_CONNECTION:latest'
      - '--set-secrets=CORS_ALLOWED_ORIGINS=CORS_ALLOWED_ORIGINS:latest'
      - '--set-env-vars=SPRING_PROFILES_ACTIVE=prod'
      - '--memory=512Mi'
      - '--min-instances=0'
      - '--max-instances=2'
      - '--timeout=120'
    waitFor: ['push-image']

# ── Secretos ────────────────────────────────────────────────
availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/SONAR_TOKEN/versions/latest
      env: 'SONAR_TOKEN'

# ── Configuración ───────────────────────────────────────────
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'

timeout: '1200s'
